# 2026-04-03 — Daily Log

## Agent Platform — CI/CD & Production Deployment Setup

### Morning: CI Fixes

**Problem:** CI workflow was failing with 19 RuboCop offenses and system-test job errors.

**Actions Taken:**
1. **Fixed lint violations** — Ran `bin/rubocop -a` to auto-fix 19 offenses in test files
2. **Disabled system-test job** — Commented out in `.github/workflows/ci.yml` since no Capybara tests exist yet
3. **Cleaned up cache files** — Added `.cache` to `.gitignore` and removed cached files from git
4. **Fixed branch mismatch** — Updated CI to run on `master` (repo uses master, not main)

**Result:** All CI jobs now passing (scan_ruby, scan_js, lint, test)

### Afternoon: GHCR (GitHub Container Registry) Setup

**Goal:** Enable production deployment via pre-built Docker images for easier distribution.

**Created Files:**

| File | Purpose |
|------|---------|
| `.github/workflows/ghcr.yml` | Builds and pushes to GHCR on every push to master |
| `docker-compose.ghcr.yml` | Production compose that pulls pre-built image |
| `DEPLOYMENT.md` | Quick start guide for users/deployers |

**GHCR Workflow Features:**
- Triggers on push to `master` or tags `v*`
- Multi-platform builds with Docker Buildx
- Caching via GitHub Actions cache
- Image tags: `master`, semver, and SHA

**Deployment Options Now Available:**

| Method | Use Case | Command |
|--------|----------|---------|
| **GHCR (Recommended)** | Users deploying production | `docker compose -f docker-compose.ghcr.yml up -d` |
| **Build Locally** | Custom builds/development | `docker compose -f docker-compose.prod.yml up --build` |
| **Development** | Local development with hot-reload | `docker compose up` |

### Documentation Cleanup

**Removed:**
- `TEST_PLAN.md` — Implementation complete, no longer needed

**Updated:**
- `ARCHITECTURE.md` — Documented GHCR as recommended deployment method
- Added `docker-compose.ghcr.yml` to production files matrix
- Updated reproducibility checklist with GHCR quick deploy steps

### Dependabot PRs

**Merged:**
- `actions/cache@v4 → v5` — Updated GitHub Actions caching

**Auto-closed by Dependabot:**
- `actions/upload-artifact@v4 → v7` — No longer needed (system tests disabled)

### Security Scanning Status

**Active:**
- ✅ Brakeman (Rails security vulnerabilities)
- ✅ bundler-audit (gem vulnerabilities)
- ✅ importmap audit (JS vulnerabilities)
- ✅ Dependabot (weekly gem and Actions updates)

### Next Steps (When Victor Returns)

1. **Wait for first GHCR image build** — Check Actions tab (~5 min build time)
2. **Create test user on VPS** — Set up separate Linux user for production deployment testing
3. **Test end-to-end deployment** — Clone repo, configure .env, run `docker-compose.ghcr.yml`
4. **Verify default login flow** — admin@localhost / changeme123 → forced password change

### Notes

- Production Dockerfile uses COPY (not volume mounts) for immutable builds
- GHCR images are public (repo is public) — no auth needed to pull
- First-time deployment requires `db:create db:migrate db:seed`
- All sensitive config via environment variables (DATABASE_URL, SECRET_KEY_BASE)

---

## Agent Memory Tools Research

### Context
Victor is evaluating memory tools for the agent-platform project. Requirements: self-hosted, n8n integration, multi-agent shared memory, open-source preference.

### Tools Researched

**1. Hindsight**
- MIT license, multi-strategy retrieval (semantic + BM25 + graph + temporal)
- Requires PostgreSQL + pgvector
- **Finding:** Primarily semantic search; BM25+ requires specific backends

**2. OpenViking**
- Apache 2.0, tiered loading (L0/L1/L2), filesystem paradigm
- Closest architectural match to Karpathy's approach
- Requires Go 1.22+, C++ toolchain, VLM + embedding model

**3. ByteRover**
- Elastic License v2, context tree as Markdown files
- Agentic Search (LLM-navigated, not vector-based)
- CLI-first, zero infrastructure, MCP server available

**4. Memary**
- MIT license, knowledge graph (Neo4j/FalkorDB)
- Multi-hop reasoning, multi-agent via user_id
- **Issue:** Last commit 2 years ago — stale project

**5. Redis Agent Memory**
- MIT license, native OpenClaw plugin
- Multi-agent scopes (shared + personal)
- Simplest setup: Docker one-liner

### Karpathy's Memory Approach (DIY)

**Core Insight:** At ~400K words (~100 articles), LLM can read full context directly — no RAG needed.

**Architecture:**
```
~/memory/
├── raw/              # Source documents (untouched)
│   ├── papers/
│   ├── codebases/
│   └── images/
├── wiki/             # LLM-compiled knowledge (auto-generated)
│   ├── _index.md     # Master index
│   ├── agents/       # Agent architecture docs
│   ├── projects/     # Project documentation
│   └── memory-tools/ # Tool comparisons
└── sessions/         # Daily working memory
    ├── 2026-04-04.md
    └── ...
```

**Workflow:**
1. **Ingest:** Add sources to `raw/`
2. **Compile:** LLM reads raw/, writes summaries + backlinks to `wiki/`
3. **Query:** LLM reads `wiki/_index.md`, finds relevant articles, reads full content

**Advantages:**
- Zero infrastructure (just files)
- Exact symbol search (LLM reads full files)
- Hierarchical organization matches mental model
- Cheap: only LLM API costs (no embeddings, no vector DB)
- Multi-agent: shared `~/memory/` directory

**Integration:**
- Rails `MemoryService` class handles LLM calls
- n8n triggers auto-compilation on new files
- OpenCode-Go for cheap LLM (MiniMax M2.5 for compile, GLM-5 for query)

**Status:** DIY Karpathy approach identified as most promising — aligns perfectly with Victor's constraints (code-heavy, self-hosted, small scale).

**Architecture Document:** `karpathy-architecture-design.md` created (921 lines)
- Clarified cache strategy: Provider-side prompt caching + book-style index navigation
- **NO EMBEDDINGS** — uses `_index.md` like a table of contents
- Session vs. LLM Context distinction documented
- Four routines: Compilation, Linting, Organization, Session Rollup

---

## Current Status & Next Steps

### Immediate Next (Today)
1. **Test GHCR Docker deployment** — Victor to create test user on VPS and deploy
2. **Verify end-to-end flow** — Clone, configure .env, run `docker-compose.ghcr.yml`

### After GHCR Testing
3. **Set up n8n** — Install and configure n8n for workflow automation
4. **DIY Memory Integration** — Build Karpathy memory system, integrate with Rails + n8n

### Blocked
- None

### Notes
- DIY Karpathy architecture ready for implementation after n8n setup
- Memory system will integrate with existing agent-platform Rails app
- Will use OpenCode-Go for cheap LLM operations (MiniMax M2.5 for compilation)

---

## Next Research Phase

**Re-evaluating Honcho** as alternative to DIY Karpathy approach:
- Dialectic reasoning (conversation-based profiling)
- Cross-session support
- Self-hosted options (community fork available)
- Comparison: structured memory vs. filesystem approach

**Research location:** `~/.openclaw/workspace/researches/`
