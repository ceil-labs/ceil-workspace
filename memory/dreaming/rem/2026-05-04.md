# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- ## ProTek Dashboard Development **Status:** Iterative fixes complete, dashboard now functional ### Issues Fixed: 1. **JavaScript initialization order** — `currentData` must be defined before `dailyData` references it 2. **Null checks in `showTab()`** — DOM element existence checks before accessing classList 3. **ProTek ≠ Zone** — Critical clarification: ProTek is company aggregate (1,152 tasks), NOT a zone 4. **Zone count corrected** — 9 operational zones: BULACAN, LAS PIÑAS-ALABANG, MAKATI, NEGROS, NORTH QC, PANAY, PASAY-PARANAQUE, PASIG-PTT, SOUTH QC 5. **Browser caching** — Multiple hard refreshes needed; added cache-busting meta tags ### Subagents Used: - MiniMax for initial fixes - [confidence=0.78 evidence=memory/2026-03-31.md:1-28]
- - Candidate: # 2026-04-03 — Daily Log ## Agent Platform — CI/CD & Production Deployment Setup ### Morning: CI Fixes **Problem:** CI workflow was failing with 19 RuboCop offenses and system-test job errors. **Actions Taken:** 1. **Fixed lint violations** — Ran `bin/rubocop -a` to auto-fix 19 offenses in test files 2. **Disabled system-test job** — Commented out in `.github/workflows/ci.yml` since no Capybara tests exist yet 3. **Cleaned up cache files** — Added `.cache` to `.gitignore` and removed cached files from git 4. **Fixed branch mismatch** — Updated CI to run on `master` (repo uses master, not main) **Result:** All CI jobs now passing (scan_ruby, scan_js, lint, test) ### Afternoon: GH [confidence=0.76 evidence=memory/2026-04-15.md:278-286]
- - Candidate: - Candidate: - ✅ Dependabot (weekly gem and Actions updates) ### Next Steps (When Victor Returns) 1. **Wait for first GHCR image build** — Check Actions tab (~5 min build time) 2. **Create test user on VPS** — Set up separate Linux user for production deployment testing 3. **Test end-to-end deployment** — Clone repo, configure .env, run `docker-compose.ghcr.yml` 4. **Verify default login flow** — admin@localhost / changeme123 → forced password change ### Notes - Production Dockerfile uses COPY (not volume mounts) for immutable builds - GHCR images are public (repo is public) — no auth needed to pull - First-time deployment requires `db:create db:migrate db:seed` - All sensitive [confidence=0.76 evidence=memory/2026-04-17.md:368-376]
