# 2026-05-04 — Daily Log

## Memoria Karpathy Alignment Pass — Full Feature Test

Completed end-to-end testing of new Memoria features as requested via Telegram test procedure.

### Test Results Summary

24/24 core tests passed. Features verified:

1. **AGENTS.md discovery** — Skill #1, accessible at `/api/skills/agents`, markdown rendered with `{{BASE_URL}}` substituted
2. **Wiki health endpoint** (`/api/wiki/health`) — Reports orphans, stubs, missingCrossRefs, untyped, staleVectors, candidatePairs, generatedAt
3. **Article-kind enum** — Valid kinds: `synthesis, entity, concept, summary, comparison, overview`. Validation works on create.
4. **Synthesize-and-file workflow** — Created synthesis article with 2 citations + 2 links, all materialized correctly
5. **Unified activity feed** (`/api/activity`) — Sorts desc by `when`, supports `kind=` filter, pagination via `nextBefore`

### Bugs Observed

1. **Activity feed `what` field is null for all events** — Should contain article slug/title for wiki events
2. **Activity feed `count` field is null** — Should return number of events in current page
3. **Link type normalization** — Specified `prerequisite`/`see-also` but API returned `references` for both. Either intentional normalization or data loss bug
4. **Synthesis article missing from activity feed** — Created at 23:54 but not visible in feed (only older events shown). Possible batching delay or missing capture

### Auth Testing (Scenarios A-E)

Previously completed on 2026-05-02, extended today:
- Anonymous tier: wiki reads open (lockWiki=false), blocked when lockWiki=true
- API key tier: "writer-tier all access" except 4 owner-only endpoints
- Owner cookie: everything
- API keys persist across owner session lock/unlock

### Memoria Endpoint Behavior

- `POST /api/wiki` requires citations (at least 1 chunkId reference)
- `POST /api/wiki/search` works anonymous
- `POST /api/search` (source chunks) requires API key
- `GET /api/wiki/health` returns full diagnostics
- Activity feed supports: `?limit=N`, `?kind=K`, `?before=ISO`
- `DELETE /api/wiki/:slug` soft-archives (returns `{ok: true, archived: true}`)

### Source Corpus Gap

No Memoria documentation ingested — only Wikipedia + HTB PDFs available. Synthesis answers grounded in API behavior observation rather than source documentation.

---

## Memoria Auth Matrix (Consolidated)

| Tier | lockWiki=false | lockWiki=true |
|------|----------------|---------------|
| Anonymous | Wiki + skills + health + graph open | Only health + skills + auth status |
| API Key | Everything except owner-only | Everything except owner-only |
| Owner Cookie | Everything | Everything |

Owner-only endpoints (return 403 for API keys):
- POST /api/settings
- POST /api/reembed/run
- /api/auth/api-keys CRUD
- PUT /api/auth/lock-wiki
- DELETE /api/auth/owner

---

**Session end:** 2026-05-04 00:46 UTC


## Evening Session — Memoria Images, Node Setup, Kimi Rate Limit (16:54–19:27 Manila)

### Memoria Inline Images Feature Test

Victor asked to test the new inline images capability for wiki articles and notes.

**Test Results:**
- **Asset upload endpoint**: `POST /api/assets/upload` — requires `scope` query param (`wiki`, `note`, or `global`)
- **Image syntax in wiki/note body**: Markdown `![alt text](/api/assets/{id})` renders correctly
- **Created wiki**: `/api/wiki/image-test-wiki` with embedded image `![Test Image](/api/assets/d4666c57-5758-4fb9-98e7-7a8274b960b1)`
- **Created note**: `/api/notes/image-test-note` with embedded image `![Test Image](/api/assets/037af3f5-a002-4ba8-9f0b-24e087b4a735)`

**Access control discovery:**
- Wiki assets: accessible without authentication (public)
- Note assets: return `401 Unauthorized` when accessed directly without auth — stricter than wiki assets

### Node Host Setup Clarification

Victor asked about setting up a node host on his MacBook Pro for browser tool access.

**Key clarifications:**
- **Port 443** is needed because Tailscale HTTPS exposes the gateway on port 443 externally; internal gateway port is 18789
- **Isolated Chrome**: no special setup needed — the node host launches it automatically in `~/.openclaw/browser/` with no logins/cookies
- **Versions don't need to match** between gateway and node — they negotiate protocol v3 at handshake
- Node is already paired in system but currently disconnected

### Kimi Rate Limit Investigation

Victor hit his Kimi quota at **6:37 PM Manila time**. Investigation:

**Dashboard evidence:**
- Two bursts of model inference calls from `openclaw` source:
  - Burst 1: 5:59:44 PM – 6:00:28 PM (10 calls, 4-5s intervals)
  - Burst 2: 6:08:52 PM – 6:09:40 PM (10 calls, 4-5s intervals)
- Total: **20 calls in ~90 seconds of wall time**
- Calls from source: `Anthropic/JS 0.90.0`

**Log gap:**
- Gateway logs are completely missing from 6:00 PM – 6:36 PM Manila time
- This is the exact window when the 20 rapid calls occurred
- Cannot determine root cause due to missing logs (possible restart/rotation)

**Fallback worked:**
- At 6:37 PM, Kimi returned `429 rate_limit_error`
- System immediately failed over to `opencode-go/minimax-m2.7`
- Conversation continued normally after failover

**Open question:** The 4-6s interval pattern suggests automated retries or a loop (active-memory plugin, subagent spawning, or stuck session), but without logs from that window the trigger remains unidentified.

---

### Node Host Setup — Work in Progress

Victor started setting up the node host on his MacBook Pro for remote browser control (isolated Chrome profile). The node is already paired in the gateway system but currently disconnected.

**Planned steps (pending):**
1. Start node host on MacBook: `openclaw node run --host srv1405873.tailcd23a1.ts.net --port 443 --tls --display-name "Victor's MacBook Pro"`
2. Approve pairing if needed
3. Verify with `openclaw nodes status`
4. Test browser tool with `target=node`

**To revisit later** — Victor paused this to investigate the Kimi rate limit issue first.

---

**Session end:** 2026-05-04 19:27 Manila / 11:27 UTC
