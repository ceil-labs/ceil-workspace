# 2026-05-01 — Daily Log

## OpenClaw Version Upgrade Tracking

**Current version:** 2026.4.24 (cbcfdf6) — stable, no issues

**Issue encountered:** When upgrading to 2026.4.29, Telegram message sending fails with:
```
channels.telegram.accounts.ceil.botToken: unresolved SecretRef
"file:filemain:/telegram/botToken"
```

**Related GitHub issues:**
- #74832 — "openclaw status fails on Telegram file SecretRef while resolving allowlist metadata" (OPEN)
- PR #74833 — fix for Telegram status accessors, still unmerged

**Root cause:** `resolveAllowFrom` tries to strict-resolve botToken SecretRef when building allowlist metadata, but CLI context lacks gateway runtime snapshot. Message send hits this path too (not just status).

**Action:** Rolled back to 4.24. Will monitor #74833 and next release (likely 4.30) for proper fix.

**Next check:** Next OpenClaw release after PR #74833 merges.

---

## Protek FSM Cleanup

- Removed 7 ProTek cron jobs from `~/.openclaw/cron/jobs.json`
- Deleted `protek-data/` (~90MB), `protek-dashboard/`, `tmp_fsm_check.sh`
- Skill files preserved but disabled in `skill.yaml`

## Secrets Audit & Cleanup

- Fixed plaintext Brave API key in `openclaw.json` → now uses SecretRef
- Removed stale `PROTEK_API_KEY` from `secrets.json` and `gateway.systemd.env`
- Audit: `clean. plaintext=0, unresolved=0, shadowed=0, legacy=0`

## UFW / SSH Hardening

- Added dual ListenAddress: Tailscale (100.108.21.107) + public (76.13.183.141)
- Added UFW allow for ISP range: `103.252.35.0/24`
- Added UFW allow for VPS provider console: `169.254.0.0/16`
- Provider web console (Hostinger) now works as SSH fallback


