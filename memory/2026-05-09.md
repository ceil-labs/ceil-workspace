# Daily Log — 2026-05-09

## Session Summary (from 2026-05-07 work)

### BOOTSTRAP.md Creation
- Created `BOOTSTRAP.md` for both Ceil and Neo workspaces
- Includes session continuity check via `sessions_list` + `sessions_history`
- Located at `~/.openclaw/workspace/BOOTSTRAP.md` and `~/.openclaw/workspace-neo/BOOTSTRAP.md`

### OpenClaw Release Evaluation
- **5.5** (May 5): Deferred — no critical fixes
- **5.6** (May 6): Deferred — low-risk patch, not critical
- **5.7** (May 7): Evaluated as worth upgrading when convenient
  - Key fixes: skills cache clearing on `/new`, context engine invalidation, Telegram polling watchdog
  - **Not fixed:** `/new` greeting bug (#68955) still open
  - Plan: skip 5.5/5.6, upgrade straight to 5.7 when convenient

### Memoria Ingest — HTB Writeups
- Ingested 25 sources (10 markdown + 15 PDF) successfully after fixing `llmBaseURL` config
- Total chunks: ~250+ across all sources
- Search verified and working

### Memoria Wiki — Enumeration Tree
- Created 3-article tree:
  - `enumeration` (overview)
  - `linux-enumeration-tools` (concept, child)
  - `windows-enumeration-tools` (concept, child)
- **Key finding:** Parent-child tree edges already render in graph view. Adding `see-also` cross-references between parent and child creates redundant visual edges. Cross-references should be reserved for non-hierarchical relationships.
- **Key finding:** Multi-linkType per cross-reference entry is NOT allowed — `linkType` is single-string per entry. To express multiple relationship types, create separate entries. This constraint is not documented in the skill.
- Removed redundant `see-also` links from Enumeration to its children
- **Feedback drafted:** `workspace/memoria-skill-feedback.md` for dev team

### Memoria DB Backup
- Created backup script: `workspace/backups/backup-memoria.sh`
- Created first backup: `memoria_db_backup_20260508_103121.dump` (1.9 MB)
- Backup is PostgreSQL custom-format dump, includes all wiki, sources, chunks, citations, notes
- Restore command documented

### Memoria Skill Update
- Added DB backup capability to memoria skill
- Natural language triggers: `/memoria db backup`, `backup db`, `backup memoria`
- Updated `SKILL.md` with backup section and `scripts/backup-memoria.sh`

### Rich Text Paste Research (Memoria UI)
- **Problem:** Pasting formatted text from Slack/VS Code/browser into Memoria textarea loses all formatting (bold, italic, code, links)
- **Root cause:** Memoria only intercepts image paste, lets default text paste happen. Browser default only uses `text/plain`.
- **Solution:** Detect `text/html` in clipboard, convert to Markdown using Turndown.js
- **Full research:** `workspace/research/rich-text-paste-memoria.md`
- **Agent prompt ready:** For implementing rich text paste in `NoteEditPage.tsx`
- Key edge cases: Slack wrapper divs, VS Code language class detection, Google Docs messy HTML

### Protek Flagged Items
- Checked pending approval queue: 26 items
- Zones: PASAY-PARANAQUE (13), NEGROS (5), BULACAN (4), NORTH QUEZON CITY (4)
- All RELOCATION orders, pending approval

## Open TODOs
- [ ] Resolve Memoria embedding provider configuration (both `embeddingProvider` and `embeddingModel` are null)
- [ ] Complete HTB writeup ingest once embedding is fixed
- [ ] Implement rich text paste in Memoria UI (agent prompt ready)
- [ ] Send memoria-skill-feedback.md to dev team
- [ ] Upgrade to OpenClaw 5.7 when convenient
- [ ] Monitor GitHub #68955 for `/new` greeting fix
- [ ] Consider automated daily Memoria DB backups (cron)

---

## OpenClaw 5.7 — Upgrade Decision: WAIT

Checked open issues since 5.7 release (May 7). Found concerning regressions.

**Blocker:** Issue #79264 — `[Regression] post-attach.update-sentinel ~9s sync block per model_call (previously 0ms) — v2026.5.7`
- Every model call now has a ~9 second synchronous block
- This would make responses painfully slow
- Labeled `regression`, active discussion
- **Decision:** Stay on 5.4 until #79264 is fixed

**Other concerning open issues:**
- #79563 — Can't specify model on web page in 5.7 (regression)
- #79462 — Codex plugin hook stalls in 5.7
- #79577 — Restart coalescing bug (has PR)

**Doctor check:** No critical issues. Minor: `NODE_COMPILE_CACHE` not set, `OPENCLAW_NO_RESPAWN` not set, legacy `plugins.allow` config key detected.

**Plan:** Monitor #79264 for fix. Revisit 5.7 upgrade once the 9s sync block regression is resolved.
