
## 2026-04-11 14:17 — Agent-Platform Progress

**Continuing work on agent-platform from previous sessions.**

### Completed Today
- Reviewed current project state: `docker-compose.yml` has full dev stack (app, postgres, redis, n8n, proxy) wired correctly
- Reviewed `Dockerfile.dev` — clean and good to go (minor note: verify `ruby:4.0.2-slim` exists on Docker Hub)
- Reviewed `nginx-proxy.conf` — routes `/`, `/n8n/`, `/n8n/webhook/` properly
- Created `postgres-init-scripts/01-create-n8n-db.sql` to auto-create n8n database on first postgres startup
- Updated both `docker-compose.yml` and `docker-compose.ghcr.yml` to mount the init script
- Advised `--no-cache` build for this test run due to prior node_modules permission issues

### Next Steps (Manual Testing)
1. `docker compose build --no-cache`
2. `docker compose up`
3. Verify Rails at `http://localhost:3000/health`
4. Verify n8n at `http://localhost:3000/n8n/` (login: admin / changeme)
5. Confirm n8n DB auto-created via init script
6. Once dev verified, test GHCR production compose

### Pending Commit
- `postgres-init-scripts/01-create-n8n-db.sql`
- `docker-compose.yml` (+1 line for init script volume)
- `docker-compose.ghcr.yml` (+1 line for init script volume)

**Status:** Ready for local dev testing. Victor will test manually before committing.

---

## 2026-04-11 16:11 — Tracking: kimi-coding/k2p5 Timeout Issue

**Context:** Neo (Telegram) experienced an LLM idle timeout on `kimi/k2p5` via the `kimi-coding` provider. After ~60s, OpenClaw auto-fallback to `minimax-m2.7` successfully. Model was later reset to `kimi/k2p5`.

**Investigation:**
- Confirmed via `openclaw logs` — primary profile `kimi-coding:default` hit `LLM idle timeout (60s)`, HTTP 408.
- OpenClaw GitHub repo search revealed **Issue #55065** (no idle timeout mechanism) plus transient kimi-coding provider latency issues.
- Victor confirmed he's on OpenClaw **v4.9** and the tool-call format bug is already resolved in this version.

**Current Assessment:**
- Only symptom observed is the **60s idle timeout** — likely transient provider-side latency from kimi-coding.
- Fallback chain is working correctly.
- Not a configuration bug on our end.

**Action:**
- Monitor Neo's session logs over the next 24–48 hours to detect if this becomes a pattern.
- If frequency increases, consider reordering fallback or switching primary model away from kimi-coding/k2p5.
