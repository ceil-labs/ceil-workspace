# 2026-03-19 - Cashflow Analyzer v2.0 Released

## Summary
Updated cashflow-analyzer skill to v2.0 with row position detection and formatting issue reporting.

## Changes Made

### New Features
1. **Row Gap Detection**
   - Detects when weekly entries have gaps (e.g., row 30 → row 32)
   - Reports exact current and expected positions
   - Currently detected: "3-15TO3-20-26" at row 32 should be at row 31

2. **Formatting Issue Detection**
   - Detects cells missing ₱ currency symbols
   - Currently detected: 7 cells need formatting
   - Provides manual fix instructions for Google Sheets

3. **Conditional Formatting Guidance**
   - Instructions for applying green (positive) / red (negative) colors
   - Step-by-step guide for Google Sheets conditional formatting

### Files Modified
- `~/.openclaw/skills/cashflow-analyzer/handler.js` - Core logic updated
- `~/.openclaw/skills/cashflow-analyzer/skill.yaml` - v2.0.0 metadata
- `~/.openclaw/skills/cashflow-analyzer/README.md` - Documentation added

### New Commands
- `/cashflow format` - Report formatting issues and row gaps
- `/cashflow fix --force --format` - Apply fixes + formatting recommendations

### Current Issues Detected
1. **Row Gap (MARCH)**: "3-15TO3-20-26" needs to move from row 32 → row 31
2. **Formatting**: 7 cells missing ₱ symbols (monthly and weekly breakdown rows)

## Next Actions
- [x] Fix row position: Move "3-15TO3-20-26" up one row in spreadsheet - DONE
- [x] Apply ₱ formatting to columns B, C, D in monthly summary - DONE
- [x] Apply conditional formatting (green/red) to Net column - DONE

---

## Updates (09:35 AM)

### gws Authentication Completed
- SSH port forwarding worked for OAuth flow
- gws now authenticated with scopes: drive, sheets, calendar, documents
- Credentials stored encrypted at ~/.config/gws/credentials.enc

### Fixes Applied via gws

**Row Position:**
- Moved "3-15TO3-20-26" from row 32 → row 31
- Gap detection now clear

**Currency Formatting (₱):**
- Applied to all monthly rows (Jan-Mar 2026)
- Applied to TOTAL 2026 row
- Applied to all weekly breakdown sections (Jan-Mar)
- Fixed March 2026 Net cell (had apostrophe prefix)

**Conditional Formatting (Green/Red):**
- Green text for positive Net values
- Red text for negative Net values
- Applied to monthly rows and weekly breakdown rows
- Fixed row 31 (3-15TO3-20-26) color formatting

### Skill Enhancement: Cell-Based Reading
- Added `getWeeklyTotalsFromCells()` function
- Reads G9 (Income) and I9 (Expenses) from weekly tabs
- Falls back to parsing if cell reading fails
- More reliable since values auto-update when weekly sheets change

### gws Commands for Future Reference

```bash
# Get sheet metadata (find sheetId for a tab)
gws sheets spreadsheets get --params '{"spreadsheetId": "SHEET_ID", "includeGridData": false}'

# Apply currency formatting
gws sheets spreadsheets batchUpdate \
  --params '{"spreadsheetId": "SHEET_ID"}' \
  --json '{"requests": [{"repeatCell": {"range": {"sheetId": ID, "startRowIndex": X, "endRowIndex": Y, "startColumnIndex": B, "endColumnIndex": E}, "cell": {"userEnteredFormat": {"numberFormat": {"type": "CURRENCY", "pattern": "\"₱\"#,##0.00"}}}, "fields": "userEnteredFormat.numberFormat"}}]}'

# Apply conditional formatting (green for positive)
gws sheets spreadsheets batchUpdate \
  --params '{"spreadsheetId": "SHEET_ID"}' \
  --json '{"requests": [{"addConditionalFormatRule": {"rule": {"ranges": [{"sheetId": ID, "startRowIndex": X, "endRowIndex": Y, "startColumnIndex": 3, "endColumnIndex": 4}], "booleanRule": {"condition": {"type": "NUMBER_GREATER", "values": [{"userEnteredValue": "0"}]}, "format": {"textFormat": {"foregroundColor": {"red": 0, "green": 0.5, "blue": 0}}}}}, "index": 0}}]}'

# Apply conditional formatting (red for negative)
gws sheets spreadsheets batchUpdate \
  --params '{"spreadsheetId": "SHEET_ID"}' \
  --json '{"requests": [{"addConditionalFormatRule": {"rule": {"ranges": [{"sheetId": ID, "startRowIndex": X, "endRowIndex": Y, "startColumnIndex": 3, "endColumnIndex": 4}], "booleanRule": {"condition": {"type": "NUMBER_LESS", "values": [{"userEnteredValue": "0"}]}, "format": {"textFormat": {"foregroundColor": {"red": 0.8, "green": 0, "blue": 0}}}}}, "index": 1}}]}'
```

## ProTek FSM Analyzer Fix (12:30 PM)

**Issue:** Duplicate Telegram messages - multiple sources sending to same channel:
1. Handler's explicit `sendTelegramMessage()` (FIXED)
2. Skill framework `output.type: telegram_message` (FIXED)
3. Cron job `delivery.mode: announce` (RETAINED)

**Fixes Applied:**

### 1. Handler Fix (handler.js)
Removed explicit Telegram send. Before:
```javascript
const telegramSent = await sendTelegramMessage(fullReport);
return { ..., telegramSent, ... };
```

After:
```javascript
// Note: Telegram output handled by skill framework
console.log('Report generated...');
return { ... }; // no telegramSent
```

### 2. Skill Output Fix (skill.yaml)
Changed output type to prevent framework auto-send:

Before:
```yaml
output:
  type: telegram_message
  format: markdown
```

After:
```yaml
output:
  type: console
  format: markdown

# Note: Cron jobs handle Telegram delivery via mode: announce
# See /home/openclaw/.openclaw/cron/jobs.json for ProTek schedules
```

### 3. Cron Jobs (RETAINED)
Cron jobs in `/home/openclaw/.openclaw/cron/jobs.json` handle Telegram delivery:
```json
"delivery": {
  "mode": "announce",
  "channel": "telegram",
  "to": "-1003821232321"
}
```

**7 Scheduled Cron Jobs:**
| Time | Command |
|------|---------|
| 07:05 | `/fsm check --cleanup` |
| 08:05 | `/fsm check --8am` |
| 10:05 | `/fsm check` |
| 12:05 | `/fsm check --facility` |
| 14:05 | `/fsm check --facility` |
| 16:05 | `/fsm check --facility` |
| 18:05 | `/fsm check --6pm --facility` |

**Result:** Single Telegram message per cron run, delivered by cron announce system.

**Files Changed:**
- `~/.openclaw/skills/protek-fsm-analyzer/handler.js`
- `~/.openclaw/skills/protek-fsm-analyzer/skill.yaml`

---

## MiniMax Model Update (10:55 AM)

**Change:** Updated default subagent model from MiniMax 2.5 to MiniMax 2.7

**Files Updated:**
- `~/.openclaw/openclaw.json` - Subagent model and fallbacks
- `AGENTS.md` - Subagent defaults reference
- `TOOLS.md` - Model reference
- `workspace-neo/TOOLS.md` - Model reference

**Configuration Changes:**
```json
// agents.defaults.subagents.model
"opencode-go/minimax-m2.5" → "opencode-go/minimax-m2.7"

// agents.defaults.model.fallbacks
Added: "opencode-go/minimax-m2.7"
Removed: "opencode-go/minimax-m2.5"

// agents.defaults.models
Updated alias from m2.5 to m2.7
```

---

## OpenCode Go Multi-Model Configuration (12:00 PM)

**Discovery:** OpenCode Go models require different API configurations based on their endpoint type.

**Models Configured:**

| Model | API Endpoint | API Config |
|-------|-------------|------------|
| MiniMax M2.7 | `/v1/messages` | `anthropic-messages` (provider default) |
| MiniMax M2.5 | `/v1/messages` | `anthropic-messages` (provider default) |
| GLM-5 | `/v1/chat/completions` | `openai-completions` (per-model override) |
| Kimi K2.5 | `/v1/chat/completions` | `openai-completions` (per-model override) |

**Key Finding:**
When models returned empty responses with 0 tokens, the issue was incorrect API configuration. MiniMax uses Anthropic Messages API while GLM/Kimi use OpenAI-compatible endpoints.

**Valid API Options in OpenClaw:**
- `anthropic-messages`
- `openai-completions`
- `openai-responses`
- `openai-codex-responses`
- `google-generative-ai`
- `github-copilot`
- `bedrock-converse-stream`
- `ollama`

**Testing Results:**
All three models now working correctly for subagents:
- ✅ MiniMax M2.7: Working (3s response)
- ✅ Kimi K2.5: Working (4s response)
- ✅ GLM-5: Working (7s response)

**Configuration Applied in `~/.openclaw/openclaw.json`:**
```json
"opencode-go": {
  "baseUrl": "https://opencode.ai/zen/go/v1/",
  "api": "anthropic-messages",
  "models": [
    { "id": "minimax-m2.7", ... },
    { "id": "minimax-m2.5", ... },
    { "id": "glm-5", "api": "openai-completions", ... },
    { "id": "kimi-k2.5", "api": "openai-completions", ... }
  ]
}
```

**Files Updated:**
- `~/.openclaw/openclaw.json` - Full provider configuration
- `AGENTS.md` - Documentation
- `TOOLS.md` - Documentation
- `workspace-neo/AGENTS.md` - Documentation  
- `workspace-neo/TOOLS.md` - Documentation

### How New Weeks Will Work
1. Create weekly tab with data
2. Add entry to monthly summary weekly breakdown
3. Run `/cashflow check` - detects new tab and any gaps
4. Run `/cashflow fix --force` - updates monthly totals
5. Run `/cashflow format` or manual gws commands - applies formatting


---

## ProTek Cron Job Fix - Explicit Skill Invocation (12:50 PM)

**Problem:** 07:05 cron job failed with `/fsm command not found`
**Root Cause:** MiniMax-2.5 treated `/fsm check --cleanup` as a shell command instead of a skill trigger
**Solution:** Updated all 7 cron job messages to explicitly mention the skill

**Changes in `/home/openclaw/.openclaw/cron/jobs.json`:**

All 7 cron jobs now use explicit skill invocation:

| Time | New Message |
|------|-------------|
| 07:05 | `Use the protek-fsm-analyzer skill to run: /fsm check --cleanup` |
| 08:05 | `Use the protek-fsm-analyzer skill to run: /fsm check --8am` |
| 10:05 | `Use the protek-fsm-analyzer skill to run: /fsm check` |
| 12:05 | `Use the protek-fsm-analyzer skill to run: /fsm check --facility` |
| 14:05 | `Use the protek-fsm-analyzer skill to run: /fsm check --facility` |
| 16:05 | `Use the protek-fsm-analyzer skill to run: /fsm check --facility` |
| 18:05 | `Use the protek-fsm-analyzer skill to run: /fsm check --6pm --facility` |

**Why This Works:**
- Explicitly tells the model to load the skill
- Removes ambiguity between shell command vs skill invocation
- Works with current MiniMax-2.5 model (and future models)

**Summary of All ProTek Fixes Today:**
1. Removed duplicate Telegram sends from handler
2. Changed skill output type to console  
3. Updated cron job messages with explicit skill invocation

Result: Single, reliable Telegram reports per cron run.
