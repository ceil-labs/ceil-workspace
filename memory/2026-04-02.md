# 2026-04-02 — Daily Log

## Memory System Migration

### Problem
- OpenClaw responses were slow (~60s delays)
- Logs showed `Failed to save messages to Honcho: TimeoutError`
- `memorySearch.multimodal` config error blocking startup

### Investigation
- Honcho memory plugin was timing out on every message sync
- QMD memory initialization failing due to multimodal embedding requirement
- `memory-core` plugin was disabled

### Fixes Applied

1. **Uninstalled Honcho plugin**
   ```bash
   openclaw plugins uninstall openclaw-honcho
   ```

2. **Disabled multimodal memory** (was causing provider adapter errors)
   - Set `agents.defaults.memorySearch.multimodal.enabled: false`
   - Set `agents.list[1].memorySearch.multimodal.enabled: false` (Neo agent)

3. **Enabled memory-core plugin**
   ```bash
   openclaw config set plugins.entries.memory-core.enabled true
   ```

4. **Verified slot configuration**
   - `plugins.slots.memory: "memory-core"` ✓

### Result
```
Memory: 13 files · 44 chunks · sources memory · plugin memory-core · 
vector ready · fts ready · cache on (64)
```

- ✅ Builtin SQLite memory engine active
- ✅ Hybrid search (vector + FTS) working
- ✅ Gemini Embeddings 2 provider configured
- ✅ No more 60-second timeouts
- ✅ Tested memory_search — returns relevant results

### Current Memory Stack
| Component | Status |
|-----------|--------|
| Plugin | memory-core (builtin) |
| Backend | SQLite + sqlite-vec |
| Embeddings | Gemini gemini-embedding-2-preview |
| Search | Hybrid (BM25 + vector) |
| Multimodal | Disabled |

### Next Steps
- Monitor for any recurrence of timeout issues
- Consider memory-lancedb if need auto-recall/capture features later
- Honcho can be reinstalled if their API stabilizes

---

## Agent-Platform Rails Project

### Progress
- ✅ Docker Compose setup with Ruby 4.0.2 + Rails 8.1.0
- ✅ PostgreSQL 16 + Redis 7 services
- ✅ Generated Rails app with `rails new . --database=postgresql --css=tailwind`
- ✅ Fixed file ownership with `user: "${UID}:${GID}"` in docker-compose.yml
- ✅ Fixed Yarn/Bundler permissions with `HOME: /app` env var
- ✅ Configured Tailscale serve URL in `config/environments/development.rb`

### Current Stack
| Component | Version |
|-----------|---------|
| Ruby | 4.0.2 |
| Rails | 8.1.3 |
| Database | PostgreSQL 16 |
| Cache | Redis 7 |
| CSS | Tailwind CSS |

### Next Steps
- Setup built-in Rails authentication (has_secure_password or new Rails 8 auth generator)
- Continue development workflow with Docker
- Commit initial Rails app structure
# 2026-04-02 — Daily Log

## Memory System Migration

### Problem
- OpenClaw responses were slow (~60s delays)
- Logs showed `Failed to save messages to Honcho: TimeoutError`
- `memorySearch.multimodal` config error blocking startup

### Investigation
- Honcho memory plugin was timing out on every message sync
- QMD memory initialization failing due to multimodal embedding requirement
- `memory-core` plugin was disabled

### Fixes Applied

1. **Uninstalled Honcho plugin**
   ```bash
   openclaw plugins uninstall openclaw-honcho
   ```

2. **Disabled multimodal memory** (was causing provider adapter errors)
   - Set `agents.defaults.memorySearch.multimodal.enabled: false`
   - Set `agents.list[1].memorySearch.multimodal.enabled: false` (Neo agent)

3. **Enabled memory-core plugin**
   ```bash
   openclaw config set plugins.entries.memory-core.enabled true
   ```

4. **Verified slot configuration**
   - `plugins.slots.memory: "memory-core"` ✓

### Result
```
Memory: 13 files · 44 chunks · sources memory · plugin memory-core · 
vector ready · fts ready · cache on (64)
```

- ✅ Builtin SQLite memory engine active
- ✅ Hybrid search (vector + FTS) working
- ✅ Gemini Embeddings 2 provider configured
- ✅ No more 60-second timeouts
- ✅ Tested memory_search — returns relevant results

### Current Memory Stack
| Component | Status |
|-----------|--------|
| Plugin | memory-core (builtin) |
| Backend | SQLite + sqlite-vec |
| Embeddings | Gemini gemini-embedding-2-preview |
| Search | Hybrid (BM25 + vector) |
| Multimodal | Disabled |

### Next Steps
- Monitor for any recurrence of timeout issues
- Consider memory-lancedb if need auto-recall/capture features later
- Honcho can be reinstalled if their API stabilizes

---

## Agent-Platform Rails Project

### Progress
- ✅ Docker Compose setup with Ruby 4.0.2 + Rails 8.1.0
- ✅ PostgreSQL 16 + Redis 7 services
- ✅ Generated Rails app with `rails new . --database=postgresql --css=tailwind`
- ✅ Fixed file ownership with `user: "${UID}:${GID}"` in docker-compose.yml
- ✅ Fixed Yarn/Bundler permissions with `HOME: /app` env var
- ✅ Configured Tailscale serve URL in `config/environments/development.rb`
- ✅ Rails server running successfully on http://localhost:3000

### Docker Development Workflow Established

**Running Rails commands inside container:**
```bash
# One-off commands
docker compose run --rm app bin/rails generate authentication
docker compose run --rm app bin/rails db:migrate

# Interactive shell
docker compose run --rm app bash

# If container already running
docker compose exec app bin/rails <command>
```

**Important:** Rails is NOT installed on host — must use Docker.

### Current Stack
| Component | Version |
|-----------|---------|
| Ruby | 4.0.2 |
| Rails | 8.1.3 |
| Database | PostgreSQL 16 |
| Cache | Redis 7 |
| CSS | Tailwind CSS |
| User | UID 1001 (not root) |

### Next Steps
- Setup built-in Rails 8 authentication generator (`bin/rails generate authentication`)
- Test authentication flow
- Continue development workflow with Docker
- Commit initial Rails app structure
