

---

## 2026-04-30 17:25 — Smooth Progress + Status Sync Deployed

### Subagent Completed (3m39s)
- **Task:** memoria-progress-fix
- **Model:** MiniMax 2.7
- **Verified:** Build passes, 38/38 tests pass, deployed to Docker

### Changes

**1. Status Sync Fix**
- `updateJobProgressByBullJobId()` now sets `status = 'processing'` whenever progress is updated
- Fixes the stale-state bug where pending jobs showed 70% progress
- DB always consistent: `status=processing` when `progress > 0`

**2. Timer-Based Smooth Progress**

**LLM Reformat step (~15s):**
- Before: single jump from 30% → 70%
- After: gradual updates every 2 seconds: 30→35→40→45→50→55→60→65→70

**Embedding step (~3-5s):**
- Before: single jump from 78% → 95%
- After: gradual updates every 1 second: 78→83→88→93→95

**3. UI Display Override**
- `pending` → "Waiting in queue... 0%"
- `processing` → actual progress bar + percentage
- `completed` → "Done 100%"
- `failed` → "Failed"

### Commit
- **Hash:** `ef70c27`
- **Pushed to:** `master`

